<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.www.repository.ChatMessageMapper">
    <insert id="saveMessage" parameterType="com.project.www.domain.ChatMessageVO">
        INSERT INTO chat_messages (chat_room_id, sender_id, message, timestamp)
        VALUES (#{chatRoomId}, #{senderId}, #{message}, #{timestamp})
    </insert>
    <select id="getMessagesByChatRoomId" resultType="com.project.www.domain.ChatMessageVO" parameterType="string">
        SELECT id, chat_room_id, sender_id, message, timestamp
        FROM chat_messages
        WHERE chat_room_id = #{chatRoomId}
        ORDER BY timestamp ASC
    </select>
</mapper>
