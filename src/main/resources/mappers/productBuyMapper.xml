<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.project.www.repository.ProductBuyMapper">

    <insert id="register">
        INSERT INTO product_buy
        (merchent_uid, customer_id, product_id, pay_price, address)
        VALUES
        (#{merchentUid}, #{customerId}, #{productId}, #{payPrice}, #{address})
    </insert>

</mapper>
